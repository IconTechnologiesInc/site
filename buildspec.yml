version: 0.2
phases:
 install:
   commands:
     - npm install -g gatsby-cli
     - npm install 
 pre_build:
   commands:
     - echo "pre_build step"
 build:
   commands:
     - echo "pre_build step "
     - gatsby build
     - cd public
     - aws s3 sync --delete . "s3://icontech.io"
 post_build:
   commands:
     - echo "post_build step"
